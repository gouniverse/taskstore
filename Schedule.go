package taskstore

// func (st *Store) Schedule(alias string, data map[string]any) error {
// 	scheduler := gocron.NewScheduler(time.UTC)

// 	scheduler.Every(10).Minutes().Do(func() {
// 		st.TaskEnqueueByAlias(alias, data)
// 	})

// 	return nil
// }
